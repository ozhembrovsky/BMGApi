@(loginForm: Form[profiles.User])

@import helper._

@main("Login") {
  
@form(routes.Auth.login) {
   <h1>Sign in</h1>

   <p>
       <input type="text" name="name" placeholder="Name" >
   </p>
   <p>
       <input type="password" name="password" placeholder="Password">
   </p>
   <p>
       <button type="submit">Login</button>
   </p>
}
}
